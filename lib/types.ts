export type ThreadInfo = {
    name: string,
    description: string,
    id: string
}

export type MessageInfo = {
    senderNick: string,
    authorNick: string,
    value: string,
    id: string
}

export type UserInfo = {
    id: string
    firstName: string,
    lastName: string,
    email: string,
    nickName: string,
}

